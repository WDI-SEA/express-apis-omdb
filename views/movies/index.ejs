  <h1>Favorite Movies</h1>
    <div>
	<ul>
	    <% results.forEach(function(result) { %>
	      <h2><a href="movies/<%= result.imdbID%>"> (<%= result.Year %>) <%= result.Title%> </a> </h2> 

	      <img src="<%= result.Poster %>">
	    <% }); %>
	    </ul>
    </div>