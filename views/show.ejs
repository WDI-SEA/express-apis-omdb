  	<div id="container">
  		<% movies.forEach(function(m){ %>
	<div>
		<h3> <%= m.Title %> (<%= m.Year %>) </h3>
		<img src="<%= m.Poster %>">
		<!-- <p> <%= m.Plot %> </p> -->
	</div>

<%}); %>
  	
	</div>