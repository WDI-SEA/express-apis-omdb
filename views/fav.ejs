<h1>Favorite Movies</h1>

<% if (faveMovieDB.length === 0) { %>
    <p>No results, <a href="/">Search again</a></p>
<% } else { %>
    <% faveMovieDB.forEach(movie => { %>
       <h3><%= movie.title %></h3>
    <% }) %>
<% } %>