<% // Render individual movie details here %>
<% // This is how to do comments in EJS, by the way... %>

<h2>Results for <%= input %></h2>

<!-- <% movie.forEach(movie => { %>
    <div>
        <h3><%= movie.name %></h3>
        <p>IMBD ID: <%= movie.imbdID %></p>
        <!-- <a href="<%= person.homeworld %>">Homeworld</a> -->
    <!-- </div>
<% }) %>  --> -->