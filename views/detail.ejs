<% // Render individual movie details here %>
<% // This is how to do comments in EJS, by the way... %>
<h1><%= movieDetail.Title %></h1>
<img src="<%= movieDetail.Poster %>" />
<form action="/faves" method="POST">
    <input type="hidden" name="title" value="<%= movieDetail.Title %>">
    <input type="hidden" name="imdbid" value="<%= movieDetail.imdbID %>">
    <input type="submit" value="add to faves">
</form>