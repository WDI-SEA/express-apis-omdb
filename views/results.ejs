<% // Render your search results here %>
<ul>
    <% data.Search.forEach((movie, index) => { %>
       <li id="hover">
            <a href="/movies/<%= movie.imdbID %>" class="result-titles"><%= movie.Title %></a>
       </li> 
    <% }) %>
</ul>


