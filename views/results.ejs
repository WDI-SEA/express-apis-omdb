<% // Render your search results here %>
<% // This is how to do comments in EJS, by the way... %>
<h2>Here's the results of your search:</h2>
<ul>
  <% data["Search"].forEach(movie => { %>
  <li>
    <%= movie["Title"] %>
  </li>

  <% }) %>
</ul>
