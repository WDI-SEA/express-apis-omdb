<% // Render your search results here %>
<% // This is how to do comments in EJS, by the way... %>

<h2>Movie Search Results</h2>
<ul>
    <% movies.forEach(function(movie){ %>
        <li><a href="/results/detail?id=<%=movie.imdbID%>"><%= movie.Title %></a></li>
    <%})%>
</ul>