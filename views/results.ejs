<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movies</title>
  </head>
  <body>
    <h1>Movies</h1>
    <% movies.forEach(function(movie){ %>

      <a href="movie/<%= movie.imdbID %>"><img src="<%= movie.Poster %>"></a>

    <% }); %>
  </body>
</html>
