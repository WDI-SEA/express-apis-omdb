<% // Render your search results here %>
<% // This is how to do comments in EJS, by the way... %>





<h2>Results for <%= movie.imdbID %></h2>

<% movies.forEach(movie => { %>
    <div>
        <h3><%= movie.Title %></h3>
        <p>imdbID: <%= movie.imdbID %></p>
        
    </div
<% }) %>  -->