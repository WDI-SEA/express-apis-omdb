<% // Render your search results here %>
<% // This is how to do comments in EJS, by the way... %>
<h2>Results for <%= input %></h2>

<div>

<% Object.keys(information).forEach(function(key) { %>
    <li><%= `${key}: ${information[key]}` %></li>
  <% }); %>
<img src="<%= information.Poster %>" />
</div>