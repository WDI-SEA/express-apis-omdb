<% // Render your search results here %>
<% // This is how to do comments in EJS, by the way... %>

<h1>Search Results for <%= input %>:</h1>
<% movies.forEach(movie => { %>
    <div>
        <h2><%= movie.Title %></h2>
        <a href='/results/details/<%= movie.imdbID %>'>details</a>
    </div>
<% }) %> 